ref_new = builtin("ref_new")
ref_get = builtin("ref_get")
ref_set = builtin("ref_set")

def id(x) = x end
a = id(3.)
b = id(())

def succ(x) = x + 1. end

x = 2.
xx = x
def x = 3. end
def f(x) =
  # y = x
  x
end

def integrator(init) =
  x = ref_new(init)
  # fun(x, dt) ->
  x
end
integrator
